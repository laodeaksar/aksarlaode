// ── Legacy entry point — superseded by app/client.tsx ─────────────────────
// TanStack Start takes over hydration via app/client.tsx + app/ssr.tsx.
// This file is intentionally empty; the build target is vinxi, not Vite's
// default index.html entry. Keeping it avoids breaking tsconfig references.
export {}
